- job:
    name: openstack-test
    parent: tox-py35
    vars:
      tox_environment:
        UPPER_CONSTRAINTS_FILE: foo.txt

- project:
    name: openstack-infra/zuul
    check:
      jobs:
        - tox-docs
        - tox-cover
        - tox-linters
        - openstack-test
        - tox-tarball
