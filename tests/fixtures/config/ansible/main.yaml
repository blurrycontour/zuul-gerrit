- tenant:
    name: tenant-one
    source:
      gerrit:
        config-repos:
          - common-config
        project-repos:
          - org/project
          - org/lookup-project
          - bare-role
