- hosts: all
  vars:
    value: "{{ lookup('csvfile', 'a file=test.csv') }}"
    #value: "{{ lookup('csvfile', 'a file=src/org/lookup_project/test.csv') }}"
  tasks:
    - debug: msg="value is {{ value }}"
