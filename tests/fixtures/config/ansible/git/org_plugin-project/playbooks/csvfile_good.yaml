- hosts: all
  vars:
    value: "{{ lookup('csvfile', 'a file=test.csv delimiter=,') }}"
    #value: "{{ lookup('csvfile', 'a file=src/org/plugin_project/test.csv') }}"
  tasks:
    - debug: msg="value is {{ value }}"
