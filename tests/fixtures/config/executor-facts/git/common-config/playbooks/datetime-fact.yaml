- hosts: localhost
  gather_facts: smart 
  tasks:
    - shell: echo "Standard output test {{ zuul.executor.src_root }}"
    - debug:
        var: date_time
    - debug:
        var: ansible_date_time