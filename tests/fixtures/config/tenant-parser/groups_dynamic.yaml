- tenant:
    name: tenant-one
    source:
      gerrit:
        config-projects:
          - common-config
        untrusted-projects:
          - exclude: project
            projects:
              - path: dynamic.py
                function: load_projects
