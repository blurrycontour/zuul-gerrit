- hosts: localhost
  tasks:
    - zuul_return:
        data:
          zuul:
            inventory:
              pod:
                ansible_connection: kubectl
                ansible_host: project-pod-1
