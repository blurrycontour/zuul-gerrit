- hosts: localhost
  tasks:
    - name: Create a k8s resource
      k8s_raw:
        state: present
        namespace: "{{ zuul_resources['namespace'].namespace }}"
        definition:
          apiVersion: v1
          kind: ConfigMap
          metadata:
            name: test-object
          data:
            config: 'test-config'
