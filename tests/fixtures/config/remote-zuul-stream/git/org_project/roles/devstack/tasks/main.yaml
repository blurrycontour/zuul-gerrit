- name: include role
  include_role:
    name: earlyrole
  when: true

- name: pause
  pause:
    seconds: 1

- name: Succeed
  delegate_to: compute1
  shell: 'echo woo'
  
- name: test block
  block:
    - name: Succeed
      become: true
      become_user: corvus
      shell: 'echo yay'
      args:
        chdir: /tmp
      when: inventory_hostname == 'controller'
  when:
    - true
    - true
