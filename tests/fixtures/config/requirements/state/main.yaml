- tenant:
    name: tenant-one
    source:
      gerrit:
        trusted-projects:
          - common-config
        untrusted-projects:
          - current-project
          - open-project
          - status-project
