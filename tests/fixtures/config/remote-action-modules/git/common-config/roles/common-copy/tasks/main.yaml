- name: Create a destination directory for copied files
  tempfile:
    state: directory
  register: test_workspace_dir

- name: Common copy
  copy:
    src: common-file
    dest: "{{ test_workspace_dir.path }}/common-file"
