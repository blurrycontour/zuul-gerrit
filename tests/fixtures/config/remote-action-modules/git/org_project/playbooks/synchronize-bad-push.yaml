- hosts: all
  tasks:
    - name: Execute common-copy role
      include_role:
        name: common-copy

    - name: Test push mode
      synchronize:
        dest: "{{ test_workspace_dir.path }}/"
        mode: push
        src: "/tmp"
        verify_host: true
