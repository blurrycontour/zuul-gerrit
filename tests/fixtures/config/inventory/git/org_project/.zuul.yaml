- project:
    name: org/project
    check:
      jobs:
        - single-inventory
        - single-inventory-list
        - group-inventory
        - hostvars-inventory
        - ansible-version27-inventory
        - ansible-version28-inventory
