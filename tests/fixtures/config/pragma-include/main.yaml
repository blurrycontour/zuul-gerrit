- tenant:
    name: tenant-one
    source:
      gerrit:
        config-projects:
          - common-config:
              include: [pipeline, job]
        untrusted-projects:
          - org/project
