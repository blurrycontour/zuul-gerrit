- tenant:
    name: tenant-one
    source:
      gerrit:
        config-projects:
          - common-config
        untrusted-projects:
          - common-jobs:
              include: [pipeline, job]
          - org/project
