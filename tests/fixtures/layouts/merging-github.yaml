- pipeline:
    name: merge
    description: Pipeline for merging the pull request
    manager: independent
    merge-failure-message: 'Merge failed'
    trigger:
      github:
        - event: pr-comment
          comment: 'merge me'
    success:
      github:
        merge: true

- project:
    name: org/project
    merge:
      jobs:
        - noop
