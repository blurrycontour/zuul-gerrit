- pipeline:
    name: check
    manager: independent
    pre-review: false
    trigger:
      gerrit:
        - event: patchset-created
    success:
      gerrit:
        Verified: 1
    failure:
      gerrit:
        Verified: -1

- job:
    name: base
    parent: null

- job:
    name: project1-test
    allow-in-pre-review-pipelines: false

- project:
    name: org/project1
    check:
      jobs:
        - project1-test
