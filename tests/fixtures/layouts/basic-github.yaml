- pipeline:
    name: check
    manager: independent
    trigger:
      github:
        - event:
            - pr-open
            - pr-change
            - pr-reopen
          branch: '^master$'
        - event: pr-comment
          comment: 'test me'
    success:
      github:
        comment: true
    failure:
      github:
        comment: true

- job:
    name: project-test1
- job:
    name: project-test2

- project:
    name: org/project
    check:
      jobs:
        - project-test1
        - project-test2
