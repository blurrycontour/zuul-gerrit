- pipeline:
    name: check
    manager: independent
    trigger:
      github:
        - event:
            - pr-open

- job:
    name: project-test1
    files:
      - '.*-requires'

- project:
    name: org/project
    check:
      jobs:
        - project-test1
