pipelines:
  - name: 'check'
    manager: IndependentPipelineManager

project-templates:
  - name: template-generic
    check:
     - '{project}-merge'

projects:
  - name: organization/project
    template:
      - name: template-generic
        project: 'myproject'
