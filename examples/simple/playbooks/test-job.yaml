# file: examples/simple/playbooks/test-job.yaml
---
- hosts: test-node
  tasks:
      - name: First task
        command: python simple/main.py
        args:
            chdir: "src/{{ zuul.project.canonical_name }}"
