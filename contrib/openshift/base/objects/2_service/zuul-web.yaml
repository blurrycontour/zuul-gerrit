- kind: Service
  apiVersion: v1
  metadata:
    name: zuul-web
  spec:
    ports:
      - name: web
        port: 9000
        targetPort: 9000
        protocol: TCP
    selector:
      app: zuul-web
    type: ClusterIP
    clusterIP: None
