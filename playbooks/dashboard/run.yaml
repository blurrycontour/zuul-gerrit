- hosts: all
  roles:
    - revoke-sudo
    - set-zuul-log-path-fact
    - role: npm
      npm_command: build:dist -- --define ZUUL_API_URL="'https://zuul.openstack.org'" --define ZUUL_BASE_HREF="{{ zuul_log_url }}/{{ zuul_log_path }}/"
