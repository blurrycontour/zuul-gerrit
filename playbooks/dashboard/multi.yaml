- hosts: all
  tasks:

    - name: Make symlinks to allow multi-tenant preview to work
      file:
        dest: .
        src: '{{ zuul.project.src_dir }}/{{ javascript_content_dir }}/{{ item }}'
        state: link
      with_items:
        - local
        - t
