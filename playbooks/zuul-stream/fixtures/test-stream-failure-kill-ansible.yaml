- name: Run some commands to show that logging works on crashed tasks too
  hosts: node
  tasks:

    - name: Run a shell task that kills its parent ansible process
      command: echo 'foo killed ansible' && sleep 1 && kill -9 $PPID
