---
- hosts: localhost
  vars:
    workspace: /tmp/tutorial-zuul
    local: true
    run_playbooks:
      - use-zuul-jobs.yaml
  tasks:
    - name: Run tutorial
      include_tasks: run-tutorial.yaml
