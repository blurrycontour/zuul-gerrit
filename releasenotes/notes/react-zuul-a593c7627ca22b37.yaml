---
features:
  - |
    The Zuul web dashboard has been rewritten in React.
upgrade:
  - |
    The Zuul web dashboard is now a single index.html. Check the install
    instruction for backward compatible rewrite rules.
