---
features:
  - |
    The GitHub trigger status filter
    :ref:`pipeline.trigger.<github source>.action.status` and pipeline
    requirements :ref:`pipeline.require.<github source>.status` now support
    regular expression matching.
