---
upgrade:
  - |
    As further integration with the scale-out scheduler is planned, the
    :attr:`zookeeper` section ``zuul.conf`` is marked required for Zuul Web.
    Please add it to your configuration.
