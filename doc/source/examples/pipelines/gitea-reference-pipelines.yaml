- pipeline:
    name: check
    manager: independent
    trigger:
      gitea:
        - event: gt_pull_request
          action:
            - opened
        - event: gt_push
    success:
      gitea:
        comment: true
        status: "success"
